# Prime Tours Folder Index

This file summarizes every file in this folder so it can be used as quick-reference context without re-reading each file in full. Keep it up to date — see the "Folder Index" rule in CLAUDE.md.

## Strategy & ground rules

| File | Summary |
|---|---|
| `CLAUDE.md` | Project ground rules and starting context. The Aug 2026 pivot to a content-led affiliate model, the six Phase 1 focus tours, the mandatory "not an operator" positioning rule and banned phrasing, identity decisions, build decisions (DDEV/GitHub/Hostinger), the content pipeline with its manual publish gate, access status, brand voice, and SEO/GEO rules. **Read first every session.** |
| `strategy.md` | Business model strategy (v1.0). Affiliate unit economics and the traffic needed to hit revenue targets; the HCU/thin-affiliate risk and why Andrew's operating history is the moat; the six selected Cape Town focus tours with rationale plus Phase 2 holds; full brand voice guide with US/UK/Europe market calibration and banned phrases; content architecture, SEO, GEO, monetisation layering, and an 18-month sequencing plan. |
| `identity.md` | Brand identity spec (v1.0) for the operator→publisher repositioning. The core reframing line; retained name plus mandatory descriptor lockup and tagline; banned operator phrasing with replacements; Andrew's named-author spec with bios and schema requirements — registration **WC7622 is current** (Western Cape Government), so present-tense "registered tourist guide" is accurate and marked up as `hasCredential`, but he lives outside South Africa and takes no guiding work, so no page may imply readers can book him and **his country of residence is never named**; trust architecture (how-we-make-money page, disclosure microcopy, the booking handoff moment); visual direction; draft page copy; pre-launch checklist. `[CONFIRM]` marks unverified facts. |
| `build.md` | WordPress build specification (v1.0). DDEV → Hostinger Business staging → production pipeline; repo structure and the "code flows up, content flows down" rule; branching and GitHub Actions deployment; why the Arizona origin is kept and mitigated with Cloudflare edge HTML caching; GeneratePress theme choice; the 9-plugin stack and why affiliate link management is non-negotiable; content architecture (`experience` CPT, `region` taxonomy, ACF fields); the seven page components; schema rules including what must NOT be marked up; tracking; AI crawler policy; the content pipeline; URL structure and the 301 redirect map; launch checklist; six-phase build sequence. |
| `affiliates.md` | Affiliate programme reference. Status table — **both approved Aug 2026**: Viator `P00148357` (30-day cookie) and GetYourGuide `2ANVBLS` (31-day cookie), both paying GBP into a UK account. Notes why revenue should be reported in GBP rather than converted ZAR. Covers why commission rate isn't the deciding factor and how to choose per experience on listing quality; link parameter construction for both platforms with warnings never to hand-edit tracking parameters; why `gyg.me` short links are avoided; ThirstyAffiliates slug conventions (name the experience, never the platform); disclosure requirements per market; open items; and the link register recording which platform was chosen per experience and why. Includes a researched **Cape Peninsula product comparison** (Aug 2026) of the top five listings on each platform, split into two like-for-like tiers: mid-range with pickup (GYG `t68762` vs Viator `d318-88021P2` — close, Viator narrowly ahead on rating and fee transparency) and budget 8-hour (GYG `t125519` and Viator `d318-58181P6` are the same tour, Viator carrying twice the reviews for $1 more). Includes a suggested two-CTA page treatment, and the campaign-code convention (one code per `/go/` link, with placement tracked in GA4 rather than in the affiliate URL — per-placement campaign codes are incompatible with link cloaking). |
| `tracking.md` | Tracking specification (v1.0, Aug 2026). Part 1 is a 20-minute execution checklist for Andrew — new GA4 property with **GBP currency and UK timezone**, 14-month retention, four event-scoped custom dimensions, key event, GTM container, Search Console link. Part 2 defines the `affiliate_click` event schema with a **closed list** of `link_position` values. Part 3 gives the GTM variable/trigger/tag structure and consent-mode wiring. Part 4 is the measurement plan — including the monthly reconciliation against commission reports required by `affiliates.md` — and Part 5 a pre-launch verification checklist. Appendix records why a new property rather than reusing the operator-era one. |
| `tracking/gtm-container.json` | **Importable GTM container** with Measurement ID `G-7K5E9PFV3T` baked in. Creates the GA4 Google Tag, the `affiliate_click` event tag with all four parameters, the custom event trigger, and five variables. Both tags require `analytics_storage` consent so they hold until Complianz grants it. Import via GTM → Admin → Import Container, merge and overwrite, review the diff before publishing. Validated: JSON parses, all variable references resolve. |
| `navigation.md` | Site structure spec (v1.0, Aug 2026). Four-item main nav (Plan Your Trip · Cape Town Tours · About · How We Make Money) with the reasoning for each and for what is deliberately excluded; how the `region` taxonomy turns national expansion into a dropdown rather than a rebuild; three-column footer and the site-wide legal strip; the full URL map of live, drafted and planned pages; and rules on `/go/` noindexing, breadcrumbs and orphan pages. Flags that privacy and terms pages are still missing and should go into the same legal review as the disclosure. |
| `index.md` | This file. |

## Content drafts

| File | Summary |
|---|---|
| `content/drafts/cape-peninsula-cape-point-tour.md` | **Flagship experience page, first draft (Aug 2026).** Template prototype for the other five focus tours. Leads with the cost section — verified SANParks conservation fees showing tours typically exclude R760 per international adult — then verdict, route detail including Chapman's Peak closure risk, seasonality table with the southern-hemisphere inversion, private vs group comparison, practical FAQs, and the booking handoff module. Booking module wired to Viator `d318-88021P2` with `d318-58181P6` as a budget secondary CTA. Facts sourced to SANParks, Chapman's Peak Drive, Cape Point and BirdLife International. Andrew's first-hand contributions are written in: the ocean-mood verdict ("good in almost any weather — the exception is a winter storm", which inverts the usual wait-for-a-clear-day advice), the small-group value judgement, the late-October booking window before the good operators fill by end of November, and the penguin activity cycle. Pricing is ZAR-first: R900 tour, R760 international conservation fees, **R1,660 real cost per adult**. **Passes the pre-publish lint clean** — no placeholders remain. Outstanding before publishing: the two `/go/` links must be created in ThirstyAffiliates from dashboard-generated Viator URLs, and two soft checks are flagged in the draft (the converted R520 budget price, and the breeding-season months). |
| `content/drafts/table-mountain.md` | **Second experience page, first draft (Aug 2026)** — Table Mountain cable car and the city tour combo. Leads with the finding that **the Cableway's own terms make third-party tickets invalid**, so the page sends readers to Webtickets and earns nothing from the highest-intent purchase. Covers verified 2026/27 pricing (R475 online vs R530 at the office), the fact that **online tickets do not skip the cable car queue**, wind closures and the 7-day ticket validity that makes advance booking safe, seasonality, Platteklip Gorge as an alternative, and whether to bundle the city tour. **Passes lint clean.** Product selected: Viator `d318-150922P4` (4.9 from 238 reviews, against GetYourGuide's 4.1 from 191). Andrew's first-hand items written in: the queue is "terrible by 11am" in Dec/Jan, Platteklip framed as a genuine hike, and a **getting-there section advising against driving** because parking at the Lower Station is worse than the queue — the most useful thing on the page that no competitor publishes. Also records two category-wide findings: the cable car ticket is almost never included in "city tour with Table Mountain" products, and a commission shopping stop is near-universal. |
| `content/drafts/about.md` | **About Andrew Fife, first draft.** The named-author page every byline links to. Carries the operator→guide reframing ("we're not a tour company any more — that's the point"), the WC7622 credential, Prime Tours 2018–2022 closed to go travelling, and the blunt "you cannot book me" line that guards against operator-impersonation. Names the commission conflict directly rather than burying it. Needs a photograph and a contact URL before publishing. |
| `content/drafts/how-we-make-money.md` | **Affiliate disclosure page, first draft. Requires professional review** for FTC / ASA / EU compliance before publishing. Plain-language explanation of the ~8% commission, that the reader pays no more, what the money does and does not buy, which links earn and which don't, and both partner IDs published openly. Lists five factual claims made on Andrew's behalf that he must confirm or cut. |
| `content/drafts/editorial-standards.md` | **Editorial standards, first draft.** How recommendations are made (four weighted criteria, commission explicitly excluded), what gets an operator dropped, the policy of publishing negative verdicts on commission-earning tours, seasonal re-verification, visible corrections, and sourcing. Includes an AI-use disclosure stating that drafting tools are used and every claim is human-verified. Each item is a promise — flagged for Andrew to keep only what he will hold to. |
| `content/drafts/homepage.md` | **Homepage, first draft.** Hero carrying the core repositioning line above the fold, trust strip, the six focus tours as a grid, planning links, and the "why trust us" section. Five of six tour cards are *coming soon* placeholders — recommendation recorded is to gate the homepage on at least three live tour pages. Needs a new `.pt-trust-strip` CSS class. |
| `content/drafts/contact.md` | **Contact page, first draft.** Leads by saying what Prime Tours *cannot* do — book, change or cancel a tour — which deflects misdirected booking enquiries and reinforces the not-an-operator positioning. Routes readers to GetYourGuide/Viator support, invites corrections and operator reports, refuses sponsored content. Flags that the old site's phone, WhatsApp and street address must **not** carry over. |
| `content/drafts/privacy.md` | **Privacy policy draft — for legal review, not publishable.** Covers what is collected via contact form, GA4 and server logs; an unusually explicit affiliate-cookie section naming both platforms and their 30/31-day lifetimes; lawful basis table; third-party processors and international transfers (US hosting, global CDN); retention; GDPR and POPIA rights. Entity resolved Aug 2026: **Andrew Fife, UK sole trader, is the data controller**, EU GDPR adopted as the single standard for all visitors. Flags that publishing a contact address conflicts with the never-name-his-location rule (**use a service address**), and leaves two questions for the reviewer: whether POPIA still applies, and whether an EU Article 27 representative is needed. |
| `content/drafts/terms.md` | **Terms of use draft — for legal review, not publishable.** Leads with the not-an-operator position and that bookings are contracts with the platform and operator, not Prime Tours; accuracy disclaimer tied to the last-verified dates; opinion vs fact distinction for operator verdicts; IP; liability. **Governing law resolved: England and Wales**, with a carve-out preserving mandatory consumer rights elsewhere. The **AI-training clause remains open** pending clarification, and must not contradict the deliberate decision to allow AI crawlers in `robots.txt`. |
| `content/briefs/` | Article briefs — topic, target query, cluster placement. Empty until the next page is commissioned. |

## Build scaffold

| File | Summary |
|---|---|
| `CLAUDE_CODE_PROMPT.md` | Paste-ready kickoff prompts for Claude Code: first a one-off prompt to clear the Drive conflict folder and commit the scaffold, then Phase 0/1 (environment, ACF field groups, the seven components, templates, verification), plus follow-ups for the flagship Cape Peninsula page and staging deployment. Ends with a table of the things Claude Code cannot work out for itself — SSH details, app password, Andrew's unverified biographical facts. |
| `README.md` | Repo entry point: layout, repo location (this Drive folder) with Drive-sync mitigations, getting-started commands, the code-up/content-down rule, branching and deployment table, content pipeline summary, and what is and isn't tracked. |
| `.gitignore` | Enforces "track code, never content". Ignores WP core, uploads, third-party plugins, secrets, database dumps, DDEV runtime, and Google Drive sync artefacts; re-includes the child theme and custom mu-plugins. |
| `.env.example` | Template for local environment variables (WP app password for the content pipeline, affiliate IDs, analytics). Also documents the GitHub Secrets required for deployment, which live in repo settings and never in a file. |
| `scripts/prepublish_lint.py` | Blocking pre-publish quality gate. Refined Aug 2026: OTA support/help/contact URLs are no longer flagged as raw affiliate links (linking a reader to a booking platform's help centre earns nothing and should not be cloaked), and the homepage is exempt from the Quick Answer requirement since its hero already front-loads the answer. Both changes regression-tested against a real product URL. Checks frontmatter, unresolved `[CONFIRM]` placeholders, banned operator phrasing, banned style phrases, raw affiliate URLs bypassing ThirstyAffiliates, missing disclosure, missing Quick Answer box and byline; warns on thin content, missing question-headings, and missing currency/distance conversions. Tested and working. Does **not** check factual accuracy — that is Andrew's gate and stays manual. |
| `.github/workflows/deploy-staging.yml` | Auto-deploys `develop` to Hostinger staging. Code only — never database or uploads. Lints PHP, rsyncs tracked paths, flushes caches, and re-asserts `blog_public 0` on every deploy so staging can never become indexable. |
| `.github/workflows/deploy-production.yml` | Deploys `main` to production behind a GitHub environment approval gate. Adds a banned-operator-phrasing grep that fails the build, a pre-deploy database backup, and post-deploy checks on homepage status and robots.txt. |

## `dev-site/` — WordPress install (DDEV project)

| File | Summary |
|---|---|
| `dev-site/.ddev/config.yaml` | DDEV project config, running on OrbStack. PHP 8.3 and MariaDB 10.11 to match Hostinger production; project root as webroot. **Mutagen disabled** (`performance_mode: none`) — it is DDEV's macOS default but would add a second async sync layer on top of Google Drive sync; OrbStack is fast enough without it. Notes that production runs LiteSpeed while DDEV uses nginx, so cache behaviour must be verified on staging rather than locally. |
| `dev-site/composer.json` | Composer manifest managing WordPress core, GeneratePress, and the nine-plugin stack via wpackagist. Core installs to the project root (not Bedrock) for straightforward shared-hosting deployment. |
| `dev-site/wp-content/themes/primetours/style.css` | GeneratePress child theme. Design tokens from `identity.md` §5 — landscape palette, serif typography, reading measure — plus styling for all seven components (`.pt-quick-answer`, `.pt-byline`, `.pt-booking`, `.pt-verdict`, `.pt-table`, `.pt-disclosure`, `.pt-verified`). |
| `dev-site/wp-content/themes/primetours/inc/tracking.php` | GTM container `GTM-WHSX6CTM` plus affiliate click tracking. Emits **Consent Mode v2 defaults immediately before the GTM snippet in the same function** — the ordering guarantees tags can't fire before consent is known, rather than depending on plugin load order. Gated by `primetours_should_load_gtm()`: production only, never local/staging (dev sessions would distort a low-traffic launch and GA4 can't remove them retroactively), never logged-in editors; override with `PRIMETOURS_GTM_FORCE` to test on staging. Pushes `affiliate_click` to the GTM data layer with destination, experience, link position and `/go/` slug. Uses a **document-level capture listener** rather than per-link binding, so links added by future components are tracked without anyone remembering to. Holds the slug→destination map (cloaked links can't reveal their destination client-side) and shows an **admin warning if any ThirstyAffiliates link is unmapped**, which would otherwise silently report as `unknown` and spoil the which-OTA-converts analysis. |
| `dev-site/wp-content/themes/primetours/functions.php` | Child theme functions: stylesheet enqueuing, front-end weight trimming (emoji scripts, conditional block CSS), theme supports, editorial image sizes, and an editor colour palette mirroring the CSS tokens. |
| `dev-site/wp-content/mu-plugins/primetours-core.php` | Content model and site policy, in mu-plugins so it survives theme changes. ACF JSON sync (the anti-drift mechanism), the `experience` CPT, `region` and `experience_type` taxonomies, Organization↔Person schema graph, AI crawler allowances in robots.txt with a staging guard, and a backstop filter forcing `rel="sponsored nofollow"` onto any raw OTA link that slips past ThirstyAffiliates. Deliberately omits Product/Offer/Review schema. |
| `dev-site/scripts/setup.sh` | Idempotent local bootstrap: Composer install, WP install, theme and plugin activation, permalinks and options, deletion of WordPress default content, and seeding of the `region` and `experience_type` taxonomy terms. |
